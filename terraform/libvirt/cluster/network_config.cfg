network-interfaces: |
  auto ${network_iface}
  iface ${network_iface} inet static
  address ${ip_address}
  network ${network}
  netmask ${netmask}
  broadcast ${broadcast}
  gateway ${gateway}
  dns-nameservers ${dns_nameservers}